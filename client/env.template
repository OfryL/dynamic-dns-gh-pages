# Polling Configuration
POLL_INTERVAL_MINUTES=15

# IP Detection API
# Options: https://api.ipify.org?format=json, https://ifconfig.me/all.json
IP_API_URL=https://api.ipify.org?format=json

# Git Configuration
GIT_USER_NAME=Dynamic DNS Bot
GIT_USER_EMAIL=bot@dynamic-dns.local

# File Path (optional - uses default if not set)
# HTML_FILE_PATH=/path/to/docs/index.html
